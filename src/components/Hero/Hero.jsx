import { ConstScroll } from "@/constant";
import React from "react";

const Hero = () => {
  return (
    <section id={ConstScroll.home} className="bg-pink-200 h-[500px]">
      Hero
    </section>
  );
};

export default Hero;
